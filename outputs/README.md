# Outputs

This directory contains public-safe derived outputs from the project:

- `annotations_labels_only.csv`: model classifications and evaluation
  metadata without copyrighted source text
- `figures/`: confusion matrices and visual summaries generated by the
  evaluation notebook

The raw poem corpus, full model responses and intermediate checkpoint files
are intentionally excluded.

